<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 联网权限：云端 AI API 与模型下载需要网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 录音权限：语音输入（系统 SpeechRecognizer 识别）需要 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 知识库/文件读取走 SAF（无需存储权限）；全盘检索需 Shizuku(root/adb) 或
         MANAGE_EXTERNAL_STORAGE，待 M7 阶段按能力等级再声明 -->

    <application
        android:name=".MemoApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ShizukuProvider：提权服务接入点（M7 文件检索 L1/L2 能力） -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- FileProvider：文件跳转（点击 AI 回复中的文件路径用系统应用打开） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
